# PyTorch (CUDA 12.1 wheel – CUDA 12.4 드라이버에서 호환 가능)
torch==2.3.0+cu121
torchvision==0.18.0+cu121
torchaudio==2.3.0+cu121
# wheel index (명령어에 포함, 여기엔 쓰지 않음)

# OpenAI CLIP
git+https://github.com/openai/CLIP.git

# 이미지 처리 및 과학 연산
Pillow>=10.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
tqdm>=4.65.0

# CLIP 종속성
ftfy>=6.0.0
regex>=2023.0.0
